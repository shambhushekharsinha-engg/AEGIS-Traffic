# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# ── Virtual environments ──────────────────────────────────────────────────────
.env
.venv/
venv/
.virtualenv/

# ── Database (runtime data — not versioned) ───────────────────────────────────
data/*
!data/.gitkeep

# ── Large raw dataset archives (not needed in repo — trained model is checked in) ──
dataset/archive*/
dataset/*.zip
dataset/*.tar
dataset/*.gz

# ── Test / scratch output ─────────────────────────────────────────────────────
verify_output.txt
check_dataset.py
verify_ucf.py
test_ucf_pipeline.py
train_ucf.py
logs/

# ── Streamlit & editor configs ────────────────────────────────────────────────
.streamlit/
.cache/
.pytest_cache/
.vscode/
*.suo
*.user

# ── Audio samples (binary — too large for git) ────────────────────────────────
dataset/Audio_Samples/*.wav

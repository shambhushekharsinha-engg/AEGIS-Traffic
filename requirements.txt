# ─── AEGIS-Traffic v8.0.0 — Production Requirements ─────────────────────────
# Vercel-optimised (FastAPI backend only).
# Streamlit dashboard is a separate service.
# Full local dev requirements → requirements-dev.txt

# Web Framework
fastapi==0.110.1
uvicorn==0.29.0
python-multipart==0.0.9

# Data / Validation
pydantic==2.7.4
pydantic-settings==2.3.4

# Database (SQLite dev / PostgreSQL prod)
sqlalchemy==2.0.28
psycopg2-binary==2.9.9
gunicorn==21.2.0

# Authentication & Security
PyJWT==2.9.0
cryptography==42.0.5

# Rate Limiting
slowapi==0.1.9

# Utilities
redis==5.0.3
celery==5.3.6
requests==2.31.0
python-dotenv==1.2.2
pandas==2.2.1
plotly==5.20.0
